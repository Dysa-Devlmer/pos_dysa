# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native — firma Android / iOS. NUNCA commitear keystores o credenciales.
# Si se filtra el keystore release, un atacante puede firmar malware con tu
# identidad. Ver docs/mobile-release-runbook.md.
.kotlin/
*.orig.*
*.jks
*.keystore
*.p8
*.p12
*.key
*.mobileprovision
credentials.json
keystore.properties
# Env local con passwords del keystore (usado por scripts/mobile-release.sh)
.env.mobile.local
android.env

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

app-example

# generated native folders
# NOTA: `/android` solía estar gitignorado (Expo default para managed workflow).
# Decidimos commitearlo para reproducibilidad, CI futuro y evitar dependencia
# de re-prebuild cada vez que alguien clona. Los build artifacts se ignoran en
# el .gitignore raíz (android/build/, android/.gradle/, android/local.properties).
/ios

{
  "name": "pos-chile",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "db:generate": "turbo run db:generate",
    "db:push": "turbo run db:push",
    "db:migrate": "turbo run db:migrate",
    "db:studio": "turbo run db:studio",
    "test": "turbo run test",
    "typecheck": "turbo run type-check",
    "health": "pnpm --filter @repo/web health",
    "audit:check": "pnpm audit --audit-level=high",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10",
    "playwright": "^1.59.1",
    "turbo": "^2.5.0"
  },
  "lint-staged": {
    "apps/web/**/*.{ts,tsx,js,jsx,mjs,cjs}": [
      "pnpm --filter web lint --fix"
    ],
    "apps/mobile/**/*.{ts,tsx}": [
      "pnpm --filter @repo/mobile lint --fix"
    ],
    "**/*.{json,md,yml,yaml}": [
      "echo 'Format check skipped (no prettier yet) — files staged'"
    ]
  },
  "packageManager": "pnpm@10.6.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "@tailwindcss/oxide",
      "esbuild",
      "prisma",
      "sharp"
    ],
    "overrides": {
      "react": "19.1.0",
      "react-dom": "19.1.0",
      "postcss": ">=8.5.10",
      "uuid": ">=14.0.0"
    }
  },
  "dependencies": {
    "@tailwindcss/oxide-darwin-x64": "4.2.2"
  }
}
